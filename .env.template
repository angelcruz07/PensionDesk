# URL pública de la app (Better Auth)
BETTER_AUTH_URL=http://localhost:3000

# Misma URL para el cliente (opcional si coincide con el origen)
NEXT_PUBLIC_BETTER_AUTH_URL=http://localhost:3000

# Secreto >= 32 caracteres: openssl rand -base64 32
# Puedes inventar una cadena larga para entorno local
BETTER_AUTH_SECRET=super_secreto_para_desarrollo_local_pension_360_12345

# PostgreSQL
DB_USER=pension
DB_PASSWORD=pension
DB_NAME=pension_360
DB_PORT=5432

# PostgreSQL (obligatorio). Con docker-compose del repo, ejemplo:
DATABASE_URL=postgresql://pension:pension@localhost:5432/pension_360

# --- VARIABLES DE STRIPE ---
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=


# Price IDs (solo servidor; se usan en `lib/auth.ts` → `subscriptionPlans`)
STRIPE_PRICE_ID_PLAN_ESENCIAL=
STRIPE_PRICE_ID_PLAN_PROFESIONAL=
STRIPE_PRICE_ID_PLAN_DESPACHO=